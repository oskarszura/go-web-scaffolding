language: go

go:
  - 1.x

node:
  - 6.2.x

install:
  - npm install

before_script:
 - npm run build

deploy:
  provider: heroku
  api_key: $HEROKU_AUTH_KEY
  app: golang-scaffolding
  on:
    all_branches: true
